<UserControl x:Class="AlyxLibInstallerWPF.Dialogs.UploadRemovalListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AlyxLibInstallerWPF" xmlns:viewmodels="clr-namespace:AlyxLibInstallerShared.Models;assembly=AlyxLibInstallerShared" d:DataContext="{d:DesignInstance Type=viewmodels:FileGlobCollection}"
      mc:Ignorable="d" 
      Width="400" Height="300"
      >

    <StackPanel Orientation="Vertical">
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,16">
            Files found in the 'game' directory of the addon using these glob patterns will be removed when the 'Remove For Upload' button is clicked.
        </TextBlock>
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <!-- TextBox fills remaining space -->
                <ColumnDefinition Width="Auto" />
                <!-- Button takes minimal space -->
            </Grid.ColumnDefinitions>
        
            <ListBox Grid.Column="0" ItemsSource="{Binding}" Padding="8" Height="300" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0" >
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <!-- TextBox fills remaining space -->
                                <ColumnDefinition Width="Auto" />
                                <!-- Button takes minimal space -->
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                    Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    HorizontalAlignment="Stretch"
                                    Margin="0"
                                    />
                            <Button Grid.Column="1"
                                    Background="{DynamicResource SystemFillColorCriticalBackgroundBrush}"
                                    Content="🗑"
                                    Command="{Binding DataContext.DeleteEntryCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}" 
                                    Margin="4,0,0,0"
                                    />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Orientation="Vertical" Grid.Column="1" VerticalAlignment="Top">
                <Button Grid.Column="1" Content="Add" Margin="10,10,0,0" HorizontalAlignment="Center" Command="{Binding AddEntryCommand}" />
                <Button Grid.Column="1" Content="View Files" Margin="10,10,0,0" HorizontalAlignment="Center" Click="Button_Click" />
            </StackPanel>

        </Grid>
    </StackPanel>
</UserControl>
