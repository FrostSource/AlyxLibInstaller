<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AlyxLibInstaller.ManageRemovalListDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlyxLibInstaller"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <!-- List -->
            <ColumnDefinition Width="*" />
            <!-- Buttons & Text -->
        </Grid.ColumnDefinitions>

        <!-- List of Paths -->
        <ListView x:Name="FileRemovalListView" Grid.Column="0" Margin="10">
            <!-- Sample item template -->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Right Panel -->
        <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top" Spacing="10">
            <TextBlock Text="Manage your file/folder list" FontSize="18" FontWeight="SemiBold"/>

            <Button Content="Add File"/>
            <Button Content="Add Folder" />
            <Button Content="Delete Selected" />

            <TextBlock Text="Use the buttons above to modify the list on the left." 
                       TextWrapping="Wrap" 
                       Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>
